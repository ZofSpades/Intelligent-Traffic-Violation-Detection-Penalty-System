# Backend Server Configuration
PORT=5000

# MySQL Database Configuration
DB_HOST=localhost
DB_USER=your_mysql_username
DB_PASSWORD=your_mysql_password
DB_NAME=trafsys
DB_CONNECTION_LIMIT=10

# Instructions:
# 1. Copy this file to .env (in the same directory)
# 2. Replace 'your_mysql_username' with your actual MySQL username
# 3. Replace 'your_mysql_password' with your actual MySQL password
# 4. Make sure the database 'trafsys' exists in your MySQL server
# 5. Never commit the .env file to Git (it's in .gitignore)
